# Server port
PORT=3000

# Your deployed contract details
CONTRACT_ADDRESS="SP1K2XGT5RNGT42N49BH936VDF8NXWNZJY15BPV4F"
CONTRACT_NAME="bitcoin-stamp"

# Chainhook node URL (used only when `CHAINHOOK_PROVIDER=local`)
CHAINHOOK_NODE_URL=http://localhost:20456

# Secret token for webhook authentication
CHAINHOOK_AUTH_TOKEN=sukanto

# Your server's external URL:
# - for `CHAINHOOK_PROVIDER=hiro`, this MUST be publicly reachable (ngrok/cloudflared/etc)
# - for `CHAINHOOK_PROVIDER=local`, localhost is fine
EXTERNAL_URL=https://won-itself-took-classified.trycloudflare.com

# Tip (cloudflared): run `npm run tunnel` and set EXTERNAL_URL to the printed `https://...trycloudflare.com` URL.

# Stacks network for predicate registration: testnet or mainnet
STACKS_NETWORK=testnet

# Faucet configuration
# WARNING: Never use a mainnet key unless you understand the risks.
FAUCET_ENABLED=true
FAUCET_ALLOW_MAINNET=false
FAUCET_PRIVATE_KEY=
FAUCET_ADDRESS=
FAUCET_AMOUNT_STX=1
FAUCET_COOLDOWN_MINUTES=1440
FAUCET_IP_COOLDOWN_MINUTES=1440

# Choose where to register chainhooks:
# - `hiro`: use the Hiro Chainhooks API (requires `HIRO_API_KEY`)
# - `local`: register against a local Chainhook node (`CHAINHOOK_NODE_URL`)
CHAINHOOK_PROVIDER="hiro"

# Hiro Platform API key (required when `CHAINHOOK_PROVIDER=hiro`)
HIRO_API_KEY="7d67ccb881aadca8bf25860fa171e84a"

# Optional override for Chainhooks API base URL (defaults to CHAINHOOKS_BASE_URL.{testnet|mainnet})
# CHAINHOOKS_BASE_URL=https://...

# Optional override for Hiro Stacks API base URL used for data fetch
# STACKS_API_BASE_URL=https://api.testnet.hiro.so
